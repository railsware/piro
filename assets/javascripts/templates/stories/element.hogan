<div class="story {{current_state}}">
  <a href="#" class="story_link_info story-title">{{name}}</a>
  {{#labelsList}}
    <a href="#" class="label">{{.}}</a>
  {{/labelsList}}
  {{#owned_by}}
    <span class="muted">(</span><a href="#">{{initials}}</a><span class="muted">)</span>
  {{/owned_by}}
</div>